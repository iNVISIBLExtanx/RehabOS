"use client";

// Stub — full implementation in feat/db-explorer branch
export function DBExplorerContainer() {
  return (
    <div className="flex flex-col items-center justify-center h-96 text-gray-400">
      <p className="text-lg font-medium">DB & SQL Explorer</p>
      <p className="text-sm mt-1">Coming soon — interactive ERD + live pglite SQL runner</p>
    </div>
  );
}
